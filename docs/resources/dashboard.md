---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "openobserve_dashboard Resource - terraform-provider-openobserve"
subcategory: ""
description: |-
  Manages an OpenObserve dashboard. The dashboard definition is expressed as a JSON string for full fidelity with complex panel configurations.
---

# openobserve_dashboard (Resource)

Manages an OpenObserve dashboard. The dashboard definition is expressed as a JSON string for full fidelity with complex panel configurations.

## Example Usage

```terraform
resource "openobserve_dashboard" "service_overview" {
  org_id      = "default"
  title       = "Service Overview"
  description = "Ingestion rate and error budget for all services."

  # Full dashboard JSON — panels, variables, and layout.
  # Export from the OpenObserve UI or compose manually.
  definition = jsonencode({
    panels    = []
    variables = {}
  })
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `org_id` (String) Organization identifier.
- `title` (String) Dashboard title shown in the UI.

### Optional

- `definition` (String) Full dashboard JSON definition. Panels, variables, and layout are encoded here.
- `description` (String) Optional description.

### Read-Only

- `dashboard_id` (String) Dashboard ID assigned by OpenObserve.
- `id` (String) Resource ID in the format `{org_id}/{dashboard_id}`.

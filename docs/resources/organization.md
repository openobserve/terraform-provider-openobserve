---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "openobserve_organization Resource - openobserve"
subcategory: ""
description: |-
  Manages an OpenObserve organization.
  ~> Note OpenObserve has no API for deleting an organization. Destroying this resource removes it from Terraform state and leaves the organization in place; delete it from the OpenObserve UI if that is what you want.
---

# openobserve_organization (Resource)

Manages an OpenObserve organization.

~> **Note** OpenObserve has no API for deleting an organization. Destroying this resource removes it from Terraform state and leaves the organization in place; delete it from the OpenObserve UI if that is what you want.

## Example Usage

```terraform
resource "openobserve_organization" "platform" {
  identifier = "platform"
  name       = "Platform Engineering"
}

# Resources in another organization reference it by identifier.
resource "openobserve_stream" "platform_logs" {
  org_id      = openobserve_organization.platform.identifier
  name        = "app_logs"
  stream_type = "logs"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Display name. Only letters, digits, spaces, and underscores are accepted.

### Optional

- `identifier` (String) Immutable organization identifier used in API paths. Derived from `name` when omitted.
- `org_type` (String) Organization type reported by the server, for example `default` or `custom`.

### Read-Only

- `id` (String) Organization identifier, same as `identifier`.
- `user_email` (String) Email of the user who owns the organization.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Organizations are imported by identifier.
terraform import openobserve_organization.platform platform
```
